/**
 * Barrel for the generated chemistry corpus.
 *
 * Maintained by `fabryka_chemii.py`: each run rewrites this file with one
 * `export * from "./chemN";` per source it processed. Empty until the first
 * run, which is why the repository layer reads whatever this module happens to
 * export rather than importing part arrays by name — a named import of a file
 * that does not exist yet would not compile.
 */
export {};
